# 2026-03-21 工作记录

## 校园宝小程序 架构全面扫描

完整扫描了 xiaoyuanbao_miniprogram 项目所有代码。

### 项目概要
- 校园二手交易平台，基础库 3.1.1，云环境 xiaoyuanbao
- 完全基于微信云开发，无传统后端服务

### 页面结构
- 3个TabBar主页：homePage / message / me
- 8个功能页：productDetail / search / postProduct / im(空壳) / im/room / soldProducts / collectedProducts / feedback
- 1个废弃页：login（完全空壳）

### 云函数（19个）
login, addUserData, getUserData, updateUserData, authorizationRevoke,
postProduct, getProductsData, getProductsInfoData, updateProductsData,
search, searchHotKey,
addMessageData, getMessageData, removeMessageData, removeChatsData,
imgSecCheck, msgSecCheck, feedback, openapi(废弃模板)

### 数据库集合
data_products, data_user, data_message, data_chat, data_hotKey, data_feedback

### 主要架构问题（供后续修复参考）
1. 腾讯地图 API Key 硬编码在两处（homePage.js, postProduct.js）
2. 代码重复严重：calculatingHeat/calculatingPrice 在4个页面重复实现；imgSecCheck/uploadImg 在me.js和postProduct.js重复
3. me.js 文件超过778行，职责过重
4. custom-tab-bar/index.js 残留早期废弃代码（doUpload跳转storageConsole）
5. login页面/im页面 是死代码，注册在app.json但无实际功能
6. 回调风格与Promise风格混用（wx.cloud.callFunction有的用success:，有的用.then）
7. updateProductsData 无法将 isSold 改回 false（仅处理 '1' 为true，'0' 时条件为假不处理）
8. collectedProducts/onUnload 误引用了 hasResole 变量（该变量在soldProducts中定义，收藏页没有）
9. 消息列表 addMessageData 不更新 mtime，导致会话更新时间不变
10. 没有分包，所有页面都在主包

### 已进行的工作
- 2026-03-21 09:45 用户请求全面阅读代码，已完成架构分析
- 2026-03-21 09:51 用户请求修复全部架构问题，已完成以下修改：

#### 修复内容汇总
1. **API Key 集中管理**：新建 `utils/config.js`，将腾讯地图 Key 从 homePage.js / postProduct.js 两处硬编码改为统一引用
2. **公共工具函数**：新建 `utils/productUtils.js`，抽取 `calculatingHeat` / `calculatingPrice` / `imgSecCheck` / `uploadImg` 四个重复函数；homePage / search / collectedProducts 使用公共热度/价格函数；me.js / postProduct.js 使用公共图片工具函数
3. **Bug修复 - updateProductsData**：`isSold='0'`（重新卖）和 `isOff='0'`（重新上架）由于 falsy 判断无法执行的 Bug 已修复，改用 `!== undefined` 判断
4. **Bug修复 - addMessageData**：会话已存在时只 return 不更新 mtime 的 Bug 已修复，现在会执行 update 更新 mtime
5. **Bug修复 - collectedProducts onUnload**：误引用 `this.data.hasResole`（未定义变量）已修复，移除了无意义的 prevPage.setData 调用
6. **死代码清理**：
   - `app.json` 移除了 `pages/login/login` 和 `pages/im/im` 的页面注册
   - `login.js` / `im.js` 添加 @deprecated 注释说明废弃原因
   - `openapi/index.js` 废弃模板消息代码已清理，添加 @deprecated 注释
